import CreatingForms from "@/components/create/CreatingForms"

export default function CreateUser() {
    return (
        <section className="w-full mx-auto h-screen bg-gray-950 overflow-hidden flex items-center justify-center">
            <CreatingForms />
        </section>
    )
}
